# IdeaForge backend runtime dependencies.
# Dev/test tools live in requirements-dev.txt.

fastapi==0.110.1
uvicorn==0.25.0
pydantic==2.12.5
pydantic-settings==2.15.0
# >=4.13 required for the async driver (pymongo.asynchronous); Motor is deprecated.
pymongo==4.18.1
dnspython==2.8.0
httpx==0.28.1
PyJWT==2.11.0
bcrypt==4.1.3
cryptography==46.0.5
# Direct LLM SDKs — the third-party scaffold proxy hop is gone (spec §Provider layer).
anthropic==1.6.0
openai==1.99.9
