# The legacy CRA dependency tree (react 19 + react-day-picker 8) has peer-dependency
# conflicts that npm refuses to resolve; yarn classic — the original installer — ignored
# them. legacy-peer-deps reproduces that behavior for npm install/ci until the Vite
# migration replaces the dependency tree.
legacy-peer-deps=true
